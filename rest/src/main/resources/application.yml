spring:
  application:
    name: rest
  datasource:
    url: jdbc:postgresql://localhost:5433/basic
    username: user
    password: Pa55word
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
